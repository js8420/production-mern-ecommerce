# Server is running at different port and the react is running on different port.

# Create new environmental variable. In react, the variable must start with "REACT_APP_"
# Whenever we make a change on .env file, we need to restart the server.
REAACT_APP_API = http://localhost:8080